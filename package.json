{
  "name": "spiritual-warfare",
  "version": "1.0.0",
  "description": "Multiplayer sword combat game - Spiritual Warfare",
  "type": "module",
  "main": "server/server.js",
  "scripts": {
    "start": "npm ci && node server/server.js",
    "dev": "npm ci && node --watch server/server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "ws": "^8.13.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "20.x"
  }
}
